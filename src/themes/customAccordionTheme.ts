import { createTheme } from "flowbite-react";

export const customAccordionTheme = createTheme({
    accordion: {
        root:{
            base: 'text-center font-extrabold text-blue-600'
        }
    } 
});